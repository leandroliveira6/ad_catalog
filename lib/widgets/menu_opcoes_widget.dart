import 'package:flutter/material.dart';

class MenuOpcoesWidget extends StatelessWidget {
  const MenuOpcoesWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.cyan
    );
  }
}